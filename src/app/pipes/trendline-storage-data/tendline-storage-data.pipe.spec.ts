import { TendlineStorageDataPipe } from './tendline-storage-data.pipe';

describe('TendlineStorageDataPipe', () => {
  it('create an instance', () => {
    const pipe = new TendlineStorageDataPipe();
    expect(pipe).toBeTruthy();
  });
});
